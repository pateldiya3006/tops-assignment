1. **Java** – Usually **compiled** into bytecode, which is then executed by the Java Virtual Machine (JVM).

2. **Python** – Usually **interpreted**, where the code is executed by the Python interpreter.

3. **JavaScript** – Can be **both compiled and interpreted**, depending on the JavaScript engine. Modern browsers often use Just-In-Time (JIT) compilation.

