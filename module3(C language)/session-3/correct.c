1.1stplayer := it is wrong because 1st is variable name.
    correct is:= firstplayer
    
2.player_age := it's correct.

3.$score := it's wrong  because $ is not allowed in normal C++ variable names. 
  correct:= score
  
4.total-marks := Invalid, because - cannot be used in a variable name.
	correct := total_marks.

5.username := it's correct.
